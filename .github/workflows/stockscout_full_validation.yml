name: StockScout Full Validation

# Final gate for the calibrated early-leader model + behavioral Groups UI.
# Manual full-run trigger: 2026-08-18 13:19 Europe/Zagreb — Group v2 + Lateral Base v1 validation
on:
  push:
    paths:
      - '.github/workflows/stockscout_full_validation.yml'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  full-scan:
    uses: ./.github/workflows/daily_screening_git_storage.yml
